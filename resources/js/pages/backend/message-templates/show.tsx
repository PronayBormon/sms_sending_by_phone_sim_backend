import { Head, Link, router } from '@inertiajs/react';
import Swal from 'sweetalert2';
export default function Show({ messageTemplate: template }: any) {
 const remove = () => Swal.fire({title: 'Delete message template?', icon: 'warning', showCancelButton: true, confirmButtonColor: '#dc3545'}).then((result) => result.isConfirmed && router.delete(`/admin/message-templates/${template.id}`));
 return <><Head title={template.title} /><div className="row"><div className="col-lg-4"><div className="card bg-white p-20 rounded-10 border border-white"><h3>{template.title}</h3><p className="text-muted">{template.sub_title || 'No subtitle'}</p><hr /><p><strong>Team:</strong> {template.team?.team_name || '—'}</p><p><strong>Creator:</strong> {template.creator ? `${template.creator.first_name} ${template.creator.last_name}` : '—'}</p><p><strong>Type:</strong> <span className="text-capitalize">{template.template_type}</span></p><p><strong>Status:</strong> {template.is_active ? 'Active' : 'Inactive'}</p></div></div><div className="col-lg-8"><div className="card bg-white p-20 rounded-10 border border-white"><div className="d-flex justify-content-between mb-20"><h3 className="mb-0">SMS Message</h3><div className="d-flex gap-2"><Link className="btn btn-primary text-white" href={`/admin/message-templates/${template.id}/edit`}>Edit</Link><button className="btn btn-danger" onClick={remove}>Delete</button><Link className="btn btn-secondary" href="/admin/message-templates">Back</Link></div></div><div className="form-control bg-light" style={{minHeight: '180px', whiteSpace: 'pre-wrap'}}>{template.message}</div>{template.variables?.length > 0 && <div className="mt-4"><strong>Variables</strong><div className="d-flex gap-2 flex-wrap mt-2">{template.variables.map((variable: string) => <span className="badge bg-info" key={variable}>{`{{${variable}}}`}</span>)}</div></div>}</div></div></div></>;
}
